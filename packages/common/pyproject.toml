[project]
name = "qubelets-common"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.10.6",
    "tomlkit>=0.14.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/qubelets"]
