[project]
name = "qubelets-dom0"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "qubelets-admin==0.1.0",
]

[tool.uv.sources]
qubelets-admin = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
bypass-selection = true
