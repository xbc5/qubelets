[project]
name = "qubelets-workspace"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "qubelets-dom0",
]

[tool.uv.sources]
qubelets-dom0 = { workspace = true }

[tool.uv.workspace]
members = ["packages/*"]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
